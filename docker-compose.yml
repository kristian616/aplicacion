version: '3.8'

services:
  web:
    build: .
    container_name: unefa_banda_guerra_app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always